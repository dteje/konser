<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.visual.conserapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Views.Login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Views.popFavs"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".Views.Cart" />
        <activity android:name=".Views.Offers" />
        <activity android:name=".Views.Home" />
        <activity android:name=".Views.SignIn" />
        <activity android:name=".Views.Detail" />
        <activity
            android:name=".Views.SandwitchCreator"
            android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar" />
        <activity android:name=".Views.SignUp" />
        <activity android:name=".Views.AdminHome"></activity>
    </application>

</manifest>